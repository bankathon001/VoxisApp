<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VoxisApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/axis_red</item>
        <item name="colorPrimaryVariant">@color/axis_red</item>
        <item name="colorOnPrimary">@color/axis_red</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/axis_red</item>
        <item name="colorSecondaryVariant">@color/axis_red</item>
        <item name="colorOnSecondary">@color/axis_red</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.VoxisApp.Fullscreen" parent="Theme.VoxisApp">
        <item name="android:actionBarStyle">@style/Widget.Theme.VoxisApp.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.VoxisApp.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>